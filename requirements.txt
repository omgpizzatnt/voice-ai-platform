# Unified requirements for all components
# Install order matters - base first, then specific overrides

# ========== Base (from Gateway) ==========
langid>=1.1.6
PyYAML>=6.0
aiohttp>=3.8.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
numpy>=1.24.0,<2.0.0
torch>=2.0.0
torchaudio>=2.0.0

# ========== WebUI ==========
gradio>=4.0.0

# ========== GPT-SoVITS Compatible Versions ==========
# These versions are tested to work with GPT-SoVITS
transformers>=4.30.0
scipy
librosa
soundfile
ffmpy

# ========== RVC Compatible Versions ==========
# RVC specific deps will be installed separately to avoid conflicts
